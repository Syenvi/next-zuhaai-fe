export type ConnectedPlatformType = {
  id: string;
  platform_id: string;
  platform_identifier: string;
  is_active: boolean;
};

export type PlatformType = {
  id: string;
  name: string;
  slug: string;
};
